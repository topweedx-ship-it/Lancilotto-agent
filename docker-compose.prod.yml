services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: trading-db-prod
    restart: always
    env_file:
      - backend/.env
    environment:
      POSTGRES_DB: trading_db
      POSTGRES_USER: trading_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backup:/backup
    networks:
      - trading-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trading_user -d trading_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    # Production resource limits
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.25'
    # Production logging
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
        labels: "service=db"

  # Trading Agent Application
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
      target: backend-build
      tags:
        - trading-agent:${TAG:-latest}
      args:
        BUILDKIT_INLINE_CACHE: 1
    container_name: trading-agent-prod
    restart: always
    env_file:
      - backend/.env
    environment:
      ENVIRONMENT: production
      DATABASE_URL: postgresql://trading_user:${POSTGRES_PASSWORD}@db:5432/trading_db
      POSTGRES_HOST: db
      LOG_LEVEL: ${LOG_LEVEL:-WARNING}  # Less verbose logging in production
    volumes:
      - app_logs:/app/logs
      - ./data:/app/data:ro
    networks:
      - trading-network
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5611/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # Production resource limits
    deploy:
      replicas: ${APP_REPLICAS:-1}  # Can be increased for high availability
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
        monitor: 30s
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
      resources:
        limits:
          memory: 4G
          cpus: '2.0'
        reservations:
          memory: 1G
          cpus: '0.5'
    # Production logging
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "10"
        labels: "service=app"
    # Security
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

  # Nginx Reverse Proxy
  nginx:
    image: nginx:1.25-alpine
    container_name: trading-nginx-prod
    restart: always
    environment:
      - DOMAIN=${DOMAIN:-your-production-domain.com}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx_logs:/var/log/nginx
    networks:
      - trading-network
    depends_on:
      - app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
        reservations:
          memory: 64M
          cpus: '0.1'
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
        labels: "service=nginx"
    # Security
    security_opt:
      - no-new-privileges:true
    read_only: true

  # Optional: Prometheus Monitoring
  prometheus:
    image: prom/prometheus:v2.45.0
    container_name: trading-prometheus-prod
    restart: unless-stopped
    volumes:
      # Use production config if exists, otherwise use default
      - ${PROMETHEUS_CONFIG:-./monitoring/prometheus.yml}:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=${PROMETHEUS_RETENTION:-200h}'
      - '--web.enable-lifecycle'
      - '--web.enable-admin-api'  # Enable admin API for maintenance
    networks:
      - trading-network
    ports:
      - "9090:9090"
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.1'
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"

  # Optional: Grafana Dashboard
  grafana:
    image: grafana/grafana:10.1.0
    container_name: trading-grafana-prod
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_INSTALL_PLUGINS: "grafana-piechart-panel,grafana-worldmap-panel"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    networks:
      - trading-network
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 128M
          cpus: '0.1'
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"

  # Optional: Backup Service
  backup:
    image: postgres:15-alpine
    container_name: trading-backup-prod
    restart: "no"  # Run on schedule, not always
    environment:
      POSTGRES_DB: trading_db
      POSTGRES_USER: trading_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-${DB_PASSWORD}}
      BACKUP_RETENTION: ${BACKUP_RETENTION_DAYS:-30}
    volumes:
      - postgres_data:/var/lib/postgresql/data:ro
      - ./backup:/backup
      - ./backup/production:/backup/production  # Production backup volume (if exists)
    networks:
      - trading-network
    command: >
      sh -c "
        echo 'Starting database backup...' &&
        pg_dump -h db -U trading_user trading_db > /backup/trading_db_$(date +%Y%m%d_%H%M%S).sql &&
        echo 'Backup completed: /backup/trading_db_$(date +%Y%m%d_%H%M%S).sql' &&
        ls -la /backup/
      "
    depends_on:
      - db
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  postgres_data:
    driver: local
  app_logs:
    driver: local
  nginx_logs:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local

networks:
  trading-network:
    driver: bridge
